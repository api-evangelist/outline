{
  "operationId": "usersDelete",
  "path": "/users.delete",
  "method": "POST",
  "summary": "Delete a user",
  "tags": [
    "Users"
  ],
  "requestSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Unique identifier for the user.",
        "format": "uuid"
      }
    },
    "required": [
      "id"
    ]
  }
}